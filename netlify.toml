[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"

# Payload admin and API routes
[[redirects]]
  from = "/admin/*"
  to = "/admin/:splat"
  status = 200
