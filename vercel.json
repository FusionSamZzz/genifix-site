{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --include=optional",
  "build": {
    "env": {
      "PAYLOAD_PUSH": "true"
    }
  }
}
