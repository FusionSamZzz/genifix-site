# Database — Neon PostgreSQL (required on Netlify)
# Also accepts DATABASE_URL as alias
DATABASE_URI=

# Required — random string 32+ chars
PAYLOAD_SECRET=change-me-in-production

# Set automatically during Netlify build only (do not add manually)
# PAYLOAD_PUSH=true

ADMIN_EMAIL=fusionsamz@gmail.com
ADMIN_PASSWORD=your-secure-password-here

NEXT_PUBLIC_SITE_URL=http://localhost:3000
